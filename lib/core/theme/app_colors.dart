import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0A0E1A);
  static const Color surface = Color(0xFF12182B);
  static const Color surfaceRaised = Color(0xFF1E2A40);

  // Accent
  static const Color primary = Color(0xFF00F5A0);
  static const Color primaryMuted = Color(0x3300F5A0);

  // Difficulty
  static const Color easy = Color(0xFF00F5A0);
  static const Color medium = Color(0xFFFFB347);
  static const Color hard = Color(0xFFFF6B6B);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8B9CC8);
  static const Color textMuted = Color(0xFF4A5578);

  // States
  static const Color amber = Color(0xFFFFB347);
  static const Color amberMuted = Color(0x33FFB347);
  static const Color success = Color(0xFF00F5A0);
  static const Color error = Color(0xFFFF6B6B);

  // Borders
  static const Color border = Color(0xFF1E2A40);
  static const Color borderSubtle = Color(0xFF151D2E);

  // Overlay
  static const Color overlay = Color(0x80000000);
}
